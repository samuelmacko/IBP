1. odavat exceptny, napr notimplemented, pretypovanie na float  atd,
    pocitat o tom
2. ako je to so storage domainamy? kolko sd moze mat vmka? kolko vmiek moze mat sd?
    to iste pre disky
3. ako je to s certifikatom? treba? staci insecure?
4, nejaky (label?) v do ktoreho bude vypuisovat: zle prihlasovacie udaje,
    zly filter, please wait, ...
5. vztah medzi networks a nics? kolko coho vmka napr?
6. pouzit re.compile

7. ako riesit filter a zoradenie poloziek v liste? ked je napr viac vmiek ale
    ukazuje iba 1.
8. nejde zoradovanie koli listu a bude treba vsetko prerobit na 1. prvok listu
9. prebrat s lukasom ze nieje dory napad mat column checkboxy v topmenu

--------------------------------------------------
otazky:
1. v clusters, co znamena allocation mac adres?
2. v clusters vsetko ostatne moze ostat? to vo viacerych
3. ako riesit snapshots? tie su ku kazdej vm, dat ich vsetky do kopy a dat im
nejake vlastne idecka?
4. ako to riesit so statistikami vmiek v diskoch (napr.), ukazovat iba 1. alebo
neukazovat vobec?
5. treba do niektorych (cluster, data center, ...) pridavat aj networks?
    niesu v zozname tabov, lebo ked ich treba vypisovat v niektorych taboch
    tak treba aby aj network mala osobitny tab

6. asi nejde storage_doamins/id/vms - neukazuje aj tam kde by malo
    nejde ani templates, malo by ukazovat ale ukazuje prazdne

7. ako zaokruhlovat b na MB? vm1 ma defined memory vo webadmine 1024 MB ale v
    api ma 1073741824 b (1073.74 MB)
8. ake je prepojenie medzi  data_centers a templates?
9. je vztah medzi templatom a data centrom taky ze template ma take data centrum
ake ma jeho cluster?


--------------------------------------------------
1. zajimavy screenshot https://www.starwindsoftware.com/blog/wp-content/uploads/2017/10/RVTools-and-vCPU-TAB.png
2. vms/<vm_guid>/statistics. jako psycho, ale bylo by gut - vms/<vm_guid>/diskattachments - ulozit id disku, pak disks/<id>/statistics a ty latency,
"hej, ktery vmko tak kurevsky zapisuje na storage?" - otazka, stejne jako "ktery vmko tak vytezuje sit?". thx. imo jsou 2 druhy pouziti -
admnistrace, tj. zjisteni, kde co je a praci s tim - smazat/presunout/..., a investigace - co jede pomalu, co neco zere,..,
ale jako ze chces "static" data a "dynamic" data, to byl smysl vety
--------------------------------------------------
ked pridavam nieco do toho co sa ma zobrazovat tak:
    1. pridam metodu do low
    2. pridam vypisanie do high
    3. v hight pridam flag (1 na viac)
    4. vo validate_filter poposuvam listy str a float

------------------------------------------------------------

imo toto:
- vms
- disks
- hosts
- storage domains
- clusters
- data centers
+ snapshots, obcas je se snapshoty problem, takze nejaky query na to jestli jsou s pameti,
  v preview atp...

object view:
- vms + sloupce, prvne "staticke", pak "dynamicke" (statistics) + snapshot name|snapshot date|snaphost status
  + console type urcite
    pokud ma napr. vice disku, vypsat v GUI vzdy prvni plus nejaky symbol znazornujici, ze existuje vice zaznamu
      priklad: foo_disk1[+]
      kliknuti na [+] by mohlo prepnout object view na typ sloupce, eg. disks view, a aplikovalo by to filter
        napr. kde vmnane = foo
- disks, vicemene ok
- hosts - nedaval bych vypis vsech vm v tomto view, spis 'pocet vm' 113[+] => odkaz na vms view a filter kde hostname = $host
- clusters - podobne jako hosts, rozhodne macrange a vypocitat allocation mac adres!
- data centers - podobne jako vyse
- templates = vms vlastne, ale nebezi
- nics + mac address, driver (virtio/em/rltk)...


typicke sysadmin "pohledy":

- hosty s urcitou verzi vdsm/libvirtu/kernelu => ktere jsou ready na update?
  tedy takovy pre-upgrade investigation
- storage, nejaky storage se zaplnuje nebo je potreba decommision urciteho
  storage hw a tudiz zmigrovata data na jinou storage
  priklad: storage Foo je 1TB, kolik tam zabiraji disky vmek data? kolik budeme potrebovat
  mista na novou storage?


------- znacky ---------------
* = vypisat len 1. alebo vypisat kazde do noveho riadku?
info = ake dalsie info?, vypisat statistiky?
------- vv toto uz je --------
vms - name, status, id, cluster version, host, memory, max memmory, os, template, statistiky vm (9), disk id (bootable), statisktiky disku (5), nic id (1.), stat. nic (8)
disks - name, vm name (pre kazdu vm novy riadok), stat. vm (9), status, id, actual size, provisioned size, format, content type, storage type, stat. disk (5)
hosts - name, vm name (pre kazdu vm novy riadok), stat. vm (9), status, id, address, cluster, nic (1.), stat. host (17)
------- vv toto este nie -----
clusters - name, version, error handling, cpu, fencing policy, firewall type, data center (info), hosts (*, info), vms (*, info), networks (*, info), affinity groups/labels
data centers - name, status, version, supported version, storage domains (*, info), networks (*, info), clusters (*, info)
storage domains - name, status, master, type, storage type, storage address, format, available space, used space, data center (info), vms (*, info), disks (*. info)
templates - name, status, version, mem. guaranteed, mem. max, cluster (info), data center (info), vms (*, info), nics (*, info), disks (*, info), storage domain (info)
nics - name, network (info), data center (info), version, vms (*, info), tempaltes (*, info)
networks - name, data center (info), nics (*, info), clusters (*, info), hosts (*, info), vms (*, info), templates (*, info)
pools - ?
volumnes - ?